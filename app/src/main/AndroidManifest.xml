<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_ghuraphera">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EGhuraPhera">
        <activity android:name=".bank"></activity>
        <activity android:name=".hospital" />
        <activity android:name=".places" />
        <activity android:name=".hotel" />
        <activity android:name=".full_details" />
        <activity android:name=".details" />
        <activity android:name=".map" />
        <activity android:name=".system_navigation" />
        <activity android:name=".signUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>